[project]
name = "evidentia"
version = "0.1.0"
description = "Multimodal grounded document intelligence system (planning + validation scaffolding)"
readme = "docs/reference/CONTRACTS.md"
requires-python = ">=3.10"
dependencies = [
  "jsonschema>=4.22.0"
]
